from pathlib import Path
content = """# Prompt: Detect Feature-Branch Cherry-Pick After Branch Cut Without False Direct-Release Violations

## Objective

Analyze the current **Python Release Tracker** implementation and enhance review-commit analysis for a specific scenario currently being reported as a `DIRECT_RELEASE` violation even though the same logical change was transferred through a feature branch after the release branch was cut.

**Important:** The Python codebase is the current source of truth. Do not refer to Java unless explicitly requested.

---

## Problem Scenario

Expected process:

1. Release branch is cut from `master`.
2. Developer has a change on a feature branch.
3. Change is committed/merged to `master`, creating SHA `123`.
4. Because the release branch was already cut, the change needs to be included in the release.
5. Normally the developer should cherry-pick SHA `123` from `master` into the release branch.

However, cherry-picking directly from `master` can cause merge conflicts.

Developers sometimes use this alternative:

```text
feature-A
   |
   v
master
   |
   +-- SHA 123

release/XXX  (already cut)
   |
   v
feature-B
   |
   | equivalent change transferred/cherry-picked
   v
release/XXX
   |
   +-- SHA 456
```

Here:

- SHA `123` exists on `master`.
- SHA `456` exists on the release side.
- SHA `123 != SHA 456`.
- The release-side change may currently be detected as `DIRECT_RELEASE`.
- Existing logic therefore reports it as a violation.

The desired behavior is:

```text
Commit Lifecycle = DIRECT_RELEASE
Compliance       = NON-VIOLATION
Category         = dedicated feature-branch-transfer classification
```

when the system can reliably establish that SHA `456` represents the same logical change transferred through the feature-branch workflow.

---

# Required Analysis

First inspect the complete current Python implementation related to:

- Generate Report
- Review Commits
- Commit analysis
- Lifecycle status
- Direct-release detection
- Cherry-pick detection
- Violation calculation
- Git/GitHub data retrieval
- Commit message parsing
- Commit SHA and parent handling
- Branch relationships
- Existing database metadata
- Existing violation category/reason/resolution
- Review Commit UI

Document the exact current flow before modifying anything.

---

# Investigate Reliable Git Signals

Determine whether the scenario can be detected reliably using Git/GitHub data.

Investigate all of the following.

## 1. Commit Message

Compare source and release-side commit messages.

Determine whether normalized message matching is useful only as supporting evidence.

Do **not** classify a commit as valid solely because messages match.

## 2. Patch Equivalence

Investigate Git mechanisms such as:

- patch-id
- equivalent diffs
- normalized patch comparison
- equivalent change detection

Determine whether patch equivalence can establish that two commits introduce the same logical code change despite different SHA IDs and parents.

Prefer a robust combination of evidence rather than a single weak heuristic.

## 3. Commit Ancestry

Analyze:

- parent SHA
- merge parents
- common ancestors
- ancestry relationships
- whether the release-side feature branch originated from the release branch
- whether the source change exists on another branch

Determine what can actually be proven from Git history.

## 4. Branch Relationships

Investigate whether the available GitHub APIs/data can establish:

- release branch origin
- feature branch origin
- whether a feature branch was derived from the release branch
- whether a source feature branch contains the corresponding master change
- whether the release-side commit originated from that feature branch

Do not assume GitHub retains an explicit branch-creation relationship if it cannot be reliably derived.

## 5. Source Commit on Master

For a candidate direct-release commit:

1. Identify the story/commit metadata.
2. Find the corresponding master change when possible.
3. Compare the release-side change against the source change.
4. Determine whether it was transferred through another feature branch.

---

# Classification Requirement

Do not simply change:

```text
DIRECT_RELEASE + violation=true
```

to:

```text
DIRECT_RELEASE + violation=false
```

Create a transparent classification.

The lifecycle should remain:

```text
DIRECT_RELEASE
```

because the branch/lifecycle behavior is still direct release.

But when reliable evidence proves that it is an equivalent feature-branch transfer, compliance should be:

```text
NON-VIOLATION
```

with a separate meaningful category.

Possible naming:

```text
FEATURE_BRANCH_CHERRY_PICK
```

or

```text
DIRECT_RELEASE_FEATURE_BRANCH_TRANSFER
```

Select the final name based on existing application terminology.

Do not use simply `OK`.

The user must be able to understand why the commit was considered non-violating.

---

# Evidence Requirements

Use strong evidence wherever possible.

Potential evidence hierarchy:

### Strong evidence

- patch-equivalent change
- identifiable source commit
- Git ancestry relationship
- clear feature-branch relationship

### Supporting evidence

- same story
- normalized commit message
- author
- timestamp
- branch naming

Do not treat weak evidence as proof.

If strong evidence cannot be established, retain the existing violation classification rather than incorrectly suppressing a genuine violation.

---

# UI Requirements

Review Commits should distinguish:

### Genuine Direct Release

```text
Commit Lifecycle: DIRECT_RELEASE
Compliance: VIOLATION
```

### Feature-Branch Transfer

```text
Commit Lifecycle: DIRECT_RELEASE
Compliance: NON-VIOLATION
Category: <new category>
```

The UI/details/tooltip should explain:

- why it is `DIRECT_RELEASE`
- why it is non-violating
- matched/source commit SHA when available
- source branch when reliably available
- detection method/evidence
- relevant story/fix-version information

Reuse the existing violation/details architecture where possible.

---

# Database Requirements

Inspect the existing `commits` table and related schema before changing it.

Determine whether additional metadata is needed, potentially including:

- source/master commit SHA
- matched equivalent commit SHA
- source branch
- release-side branch
- detection method
- lifecycle
- violation flag
- violation category
- reason
- resolution

Do not add unnecessary columns.

If schema changes are required:

1. Document them.
2. Preserve existing data.
3. Ensure Generate Report populates them.
4. Ensure Review Commits consumes persisted values consistently.

---

# Performance Requirements

Generate Report processes many commits.

Do not introduce expensive GitHub API calls for every commit without analysis.

Investigate:

- reusable existing API responses
- batching
- caching
- parallel processing
- existing Python concurrency/executor patterns

The new logic must not materially degrade report-generation performance.

---

# Mandatory Test Scenarios

## Scenario 1 — Genuine Direct Release

```text
master: commit A
release: unrelated commit B
```

Expected:

```text
DIRECT_RELEASE
VIOLATION
```

## Scenario 2 — Normal Master-to-Release Cherry-Pick

```text
master: commit A
release: equivalent cherry-pick of A
```

Expected:

```text
CHERRY_PICK
NON-VIOLATION
```

## Scenario 3 — Feature-Branch Transfer After Branch Cut

```text
master: commit A
release branch already exists
release-feature: equivalent change transferred from source feature branch
release: resulting commit B
```

Expected:

```text
DIRECT_RELEASE
NON-VIOLATION
FEATURE_BRANCH_CHERRY_PICK / equivalent category
```

## Scenario 4 — Same Message, Different Code

Same/similar commit message but different patch.

Expected:

```text
Do not classify as non-violation.
```

## Scenario 5 — Same Story, Different Implementation

Same story but unrelated change.

Expected:

```text
Do not classify as non-violation.
```

## Scenario 6 — Similar Metadata, No Strong Relationship

Same author/story/message but no reliable source/patch/ancestry relationship.

Expected:

```text
DIRECT_RELEASE
VIOLATION
```

## Scenario 7 — Existing Cherry-Pick

Verify existing valid cherry-pick detection remains unchanged.

---

# Implementation Plan Before Coding

Before modifying code, create a detailed plan covering:

1. Current direct-release detection flow
2. Current cherry-pick detection flow
3. Existing GitHub API usage
4. Existing commit metadata
5. Existing DB fields
6. Proposed detection strategy
7. Git APIs required
8. Patch-equivalence approach
9. Branch/ancestry approach
10. New classification/category
11. Database changes
12. Backend changes
13. UI changes
14. Performance impact
15. Unit/integration tests
16. Backward compatibility
17. Risks and mitigations

Do not implement until this plan is completed and reviewed.

---

# Acceptance Criteria

- [ ] Genuine direct releases remain violations.
- [ ] Existing valid cherry-picks remain non-violations.
- [ ] Feature-branch transfer/cherry-pick scenarios are detected when sufficient evidence exists.
- [ ] Lifecycle remains `DIRECT_RELEASE` for this scenario.
- [ ] A dedicated non-violation classification/category is persisted and displayed.
- [ ] UI explains why the commit is non-violating.
- [ ] Commit-message matching alone is never sufficient.
- [ ] Story matching alone is never sufficient.
- [ ] Different SHA IDs are handled correctly.
- [ ] Existing review-commit logic is preserved.
- [ ] Existing violation rules remain unchanged for unrelated scenarios.
- [ ] Database persistence is consistent.
- [ ] Generate Report performance is protected.
- [ ] All mandatory scenarios have automated tests.
- [ ] No Java comparison is introduced unless explicitly requested.

## Final Instruction

Analyze the current Python Release Tracker codebase first and determine the most reliable technically feasible solution using Git/GitHub information.

Do not hide violations merely to reduce violation counts.

The goal is **accurate governance classification with transparent evidence**.
"""
path = Path("/mnt/data/direct_release_feature_branch_cherrypick_prompt.md")
path.write_text(content, encoding="utf-8")
print(path)
