from pathlib import Path

content = """# Jira Cloud + Legacy iTrack Dual-Mode Migration

## Objective

Migrate all existing iTrack integrations used by Release Tracker to AT&T Jira Cloud while preserving the existing functionality and keeping legacy iTrack fully operational.

The application must support both:

1. **Jira Cloud** — new/default backend
2. **Legacy iTrack** — existing backend for older releases/fix versions

The user must select the backend from the **Generate Report** page.

---

## 1. Generate Report UI Switch

Add a compact switch near the Generate Report controls:

`Data Source: [ Jira Cloud | Legacy iTrack ]`

Requirements:

- Default = **Jira Cloud**
- Jira Cloud selection uses the new Jira Cloud client.
- Legacy iTrack selection uses the existing iTrack client.
- The selected source controls the complete Generate Report story/fix-version/team data flow.
- Existing Generate Report behavior must otherwise remain unchanged.
- The UI should clearly show which backend is active.
- Follow the existing UI design system.

---

## 2. Do Not Modify the Existing iTrack Client

This is mandatory.

Do not rewrite, replace, or heavily refactor the existing iTrack client to support Jira Cloud.

Create a **separate Jira Cloud client/module**.

Prefer a clean provider abstraction:

```text
Generate Report
      |
      v
Issue/Story Provider
      |
      +---- JiraCloudClient
      |
      +---- LegacyITrackClient
```

Business logic should not contain duplicated provider-specific implementation.

---

## 3. Complete iTrack Use-Case Inventory

Before coding, analyze the entire codebase and identify every existing iTrack dependency.

Search for:

- iTrack client imports
- iTrack service calls
- REST calls
- iTrack queries
- story retrieval
- fix-version retrieval
- team/team-key lookup
- scrum-team lookup
- story status
- reporter
- acceptance/completion status
- story-number lookup
- story metadata
- report-generation dependencies
- review-commit dependencies
- release-readiness dependencies
- export/Excel dependencies
- asynchronous/background iTrack calls
- retries and error handling
- configuration/environment variables
- tests and mocks

Do not limit the analysis to Generate Report.

**Acceptance criterion:** no existing iTrack use case may be accidentally omitted.

---

## 4. Jira Cloud Integration

The Jira Cloud environment is:

```text
https://atlassian.net
```

The actual company Jira Cloud hostname/path and token will be provided separately.

Do not hard-code credentials or tokens.

Use environment/configuration management.

Create a dedicated Jira Cloud client and keep it separate from the existing iTrack implementation.

---

## 5. Authentication

A Jira Cloud API token will be supplied.

Use the approved authentication mechanism for the Jira Cloud APIs.

Requirements:

- Token must remain outside source code.
- Token must not be logged.
- Authorization headers must not be logged.
- Secrets must not appear in exceptions or UI.
- Follow company security requirements.
- Verify the exact Jira Cloud authentication format rather than assuming it is identical to iTrack.

---

## 6. Jira Cloud API Mapping

Before implementation, map every existing iTrack operation to its Jira Cloud equivalent.

For each use case document:

| iTrack Use Case | Existing API/Query | Jira Cloud API | Parameters | Response | Mapping |
|---|---|---|---|---|---|
| Story retrieval | Analyze source | Identify official Jira API | Team/Fix Version/etc. | Issue data | Required |
| Fix version | Analyze source | Identify official Jira API | Project/version | Version data | Required |
| Scrum team | Analyze source | Identify Jira equivalent | Issue/story | Team data | Required |
| Story status | Analyze source | Identify Jira API | Issue | Status | Required |
| Reporter | Analyze source | Identify Jira API | Issue | Reporter | Required |
| Other use cases | Discover | Map individually | As required | As required | Required |

Do not guess API equivalence. Verify the official Jira Cloud API contract.

---

## 7. Preserve Existing Business Logic

This is an integration migration, not a business-rule redesign.

Preserve the existing behavior for:

- Team selection
- Team key handling
- Fix-version handling
- Story retrieval
- Story filtering
- Story status
- Reporter
- Scrum team
- Story persistence
- Commit analysis
- Violation logic
- Lifecycle status
- Release readiness
- Deployment readiness
- Branch-cut validation
- Excel/export behavior
- Generate Report
- Refresh behavior
- Database cleanup/reload behavior

Only the external issue-tracking source should change.

---

## 8. Fix Version — CRITICAL

The complete fix-version string must be preserved exactly.

Do not:

- truncate
- remove spaces
- remove meaningful internal whitespace
- abbreviate
- split incorrectly
- alter case unnecessarily
- normalize it incorrectly

Verify the value throughout:

```text
UI
 -> Backend
 -> Jira API
 -> Database
 -> Analysis
 -> UI
 -> Excel Export
```

Explicitly test fix versions containing spaces and other valid characters.

---

## 9. Provider Selection

Do not scatter provider logic throughout the business layer.

Prefer a provider/factory/strategy abstraction following the existing architecture.

Conceptually:

```python
provider = provider_factory.get_provider(source)

stories = provider.fetch_stories(
    team_key=team_key,
    fix_version=fix_version
)
```

The exact implementation must follow project conventions.

Avoid unnecessary framework layers.

---

## 10. Generate Report Flow

Trace the existing flow completely before changing it.

Expected conceptual flow:

```text
UI
 ↓
Team
 ↓
Fix Version
 ↓
Data Source Switch
 ↓
Controller/Route
 ↓
Service
 ↓
Selected Provider
 ↓
Story Retrieval
 ↓
Existing Database Cleanup
 ↓
Story Persistence
 ↓
Component Retrieval
 ↓
Commit Analysis
 ↓
Release Validation
 ↓
Report Generation
 ↓
UI
```

The actual sequence must be derived from the current working implementation.

The Jira switch should change only the external issue source.

---

## 11. Database

Analyze all tables and columns populated from iTrack.

Identify:

- tables
- columns
- primary/unique keys
- team/team-key fields
- fix-version fields
- story number
- status
- reporter
- scrum team
- timestamps
- source metadata

Do not unnecessarily change the database schema.

If Jira Cloud requires a new field, document why before introducing it.

---

## 12. Existing Cleanup and Reload Behavior

Preserve existing data-refresh semantics.

Where the current flow deletes records for:

```text
Team Key + Fix Version
```

before fetching and storing fresh data, retain the same behavior.

Ensure:

- stale iTrack data is not incorrectly reused when Jira Cloud is selected.
- unrelated teams/releases are never deleted.
- delete conditions remain exact.
- fresh Jira data is persisted using the existing business model.

---

## 13. Legacy iTrack Compatibility

Legacy iTrack must remain fully usable because some older releases/fix versions still depend on it.

Conceptually:

```text
Older Release/Fix Version
        |
        +--> Legacy iTrack

New Release/Fix Version
        |
        +--> Jira Cloud
```

The UI switch is the explicit selection mechanism.

Do not automatically infer the backend from fix-version naming unless separately approved.

---

## 14. Error Handling

Handle Jira Cloud errors without exposing secrets.

Cover:

- authentication failure
- invalid/expired token
- timeout
- Jira unavailable
- rate limiting
- invalid JQL/query
- missing project/version
- issue not found
- permission errors
- malformed response
- unexpected response
- network failure

Provide useful application-level errors.

Do not expose tokens, authorization headers, or sensitive configuration.

---

## 15. Logging

Add useful informational/debug logs around provider selection and Jira operations.

Examples:

```text
Generate Report started
Selected source: Jira Cloud
Selected team key: <safe value>
Selected fix version: <safe value>
Fetching stories from Jira Cloud
Jira Cloud retrieval completed
Stories retrieved: N
```

Never log:

- API tokens
- Authorization headers
- passwords
- cookies
- secrets

---

## 16. Testing

### Jira Cloud

Test:

- authentication
- story retrieval
- fix version
- team filtering
- status
- reporter
- scrum team
- empty results
- timeout
- rate limit
- malformed response
- persistence
- Generate Report integration

### Legacy iTrack

Ensure existing tests continue to pass.

### Provider Selection

Verify:

```text
Jira Cloud selected -> JiraCloudClient called
Legacy selected     -> LegacyITrackClient called
```

### Fix Version

Test a value such as:

```text
Example Release 2026 08
```

and verify the exact value survives the entire flow.

---

## 17. End-to-End Validation

### Scenario A — Jira Cloud

```text
Select Team
Select Fix Version
Select Jira Cloud
Generate Report
Verify Jira Cloud calls
Verify stories
Verify DB
Verify analysis
Verify release readiness
Verify UI/export
```

### Scenario B — Legacy iTrack

```text
Select Team
Select Fix Version
Select Legacy iTrack
Generate Report
Verify iTrack calls
Verify stories
Verify DB
Verify analysis
Verify release readiness
Verify UI/export
```

Where equivalent data exists, compare final business results.

---

## 18. No Regression

Do not break:

- Review Commits
- Release Readiness
- Deployment Readiness
- Branch Cut Validation
- Dashboard
- Scan History
- Settings
- Excel Export
- Commit Analysis
- Violation detection
- Lifecycle status
- Teams notifications
- Existing DB behavior
- Legacy iTrack

Keep Jira migration isolated.

---

# 19. Implementation Plan — REQUIRED BEFORE CODING

Do not immediately modify the code.

First create a detailed plan.

### Phase 1 — Discovery

Identify every iTrack usage, API, query, DB interaction, UI entry point, configuration, and test.

### Phase 2 — Existing Behavior

Document the current iTrack flow from UI to backend to DB/output.

### Phase 3 — Jira Mapping

Map every existing iTrack operation to an official Jira Cloud API.

### Phase 4 — Client Architecture

Design the separate Jira Cloud client and provider abstraction.

### Phase 5 — UI

Add the Jira Cloud / Legacy iTrack switch.

### Phase 6 — Integration

Connect provider selection to the existing business flow.

### Phase 7 — Database

Validate cleanup, persistence, and reload behavior.

### Phase 8 — Testing

Add unit, integration, and end-to-end coverage.

### Phase 9 — Regression

Verify all existing Release Tracker functionality.

### Phase 10 — Final Review

Confirm that no iTrack use case was missed.

---

# 20. Definition of Done

- [ ] Separate Jira Cloud client implemented.
- [ ] Existing iTrack client remains intact.
- [ ] Jira Cloud is default.
- [ ] Legacy iTrack remains selectable.
- [ ] Generate Report switch controls provider.
- [ ] Every iTrack use case identified.
- [ ] Every applicable use case mapped to Jira Cloud.
- [ ] Business logic unchanged.
- [ ] Fix-version strings preserved exactly.
- [ ] Team/team-key behavior preserved.
- [ ] Story retrieval works in both modes.
- [ ] Story status works in both modes.
- [ ] Reporter works in both modes.
- [ ] Scrum-team lookup works in both modes.
- [ ] Database cleanup/reload preserved.
- [ ] Generate Report works end-to-end in both modes.
- [ ] Review Commits remains functional.
- [ ] Release Readiness remains functional.
- [ ] Deployment Readiness remains functional.
- [ ] Branch Cut Validation remains functional.
- [ ] Dashboard remains functional.
- [ ] Scan History remains functional.
- [ ] Settings/Excel export remains functional.
- [ ] Existing iTrack tests pass.
- [ ] Jira Cloud tests pass.
- [ ] Provider-selection tests pass.
- [ ] Fix-version edge-case tests pass.
- [ ] No credentials are hard-coded.
- [ ] No secrets are logged.
- [ ] Error handling is implemented.
- [ ] Documentation updated.
- [ ] Final review confirms no unnecessary unrelated changes.

---

# 21. Final Instruction to the Development Agent

Treat the current working Release Tracker implementation as the **behavioral reference**.

The objective is:

> Add Jira Cloud as a new issue-tracking provider while preserving the legacy iTrack provider and all existing Release Tracker business behavior.

Before coding:

1. Analyze the complete codebase.
2. Find every iTrack dependency.
3. Trace every iTrack call from UI through backend to DB.
4. Map every use case to Jira Cloud.
5. Create a detailed implementation plan.
6. Identify risks and compatibility concerns.
7. List all files that will change.
8. Only then implement.

Do not remove legacy iTrack.

Do not rewrite the existing iTrack client.

Do not change unrelated business logic.

Do not truncate or alter fix-version values.

Do not assume Jira API equivalence without verification.

The final architecture should conceptually be:

```text
                    Generate Report
                           |
                    Data Source Switch
                     /               \\
                    /                 \\
             Jira Cloud          Legacy iTrack
                  |                     |
            New Jira Client       Existing Client
                  \\                     /
                   \\                   /
                    Existing Business Logic
                             |
                    Existing DB/Reports/UI
```

The result must provide a safe transition path from legacy iTrack to Jira Cloud without regression.
"""

path = Path("/mnt/data/jira_cloud_dual_mode_migration_prompt.md")
path.write_text(content, encoding="utf-8")
print(path.exists(), path)
