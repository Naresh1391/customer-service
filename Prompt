Release Tracker – Consolidated Enhancements

Please implement the following changes in the existing Release Tracker application.

Keep the implementation focused. Do not make unrelated changes or introduce unnecessary architecture. The existing application must remain stable and all current functionality must continue to work.

If anything is unclear or conflicts with the existing AADLC implementation, ask me before making assumptions.

1. Align with AADLC / Existing Applications

Compare the Release Tracker frontend/BFF/backend implementation with the actual AADLC project and other existing AADLC tools.

Identify and correct meaningful differences in:

* naming conventions;
* configuration conventions;
* authentication/authorization patterns;
* application settings;
* local configuration;
* deployment configuration;
* UI terminology where appropriate.

Follow the existing AADLC standards/conventions rather than creating a Release Tracker-specific pattern.

⸻

2. Entra ID Authorization

Use the existing AI Efficiency Tracker implementation as the reference for Entra group-based authorization.

Admin group:

AP-AIDLC-AIFO-Admin

I will provide the Group/Object ID separately.

Only two roles are required:

* Admin
* Viewer

Remove the Developer role completely.

Permissions

Viewer

* View application data.
* View Team Administration.
* No Add/Edit/Delete actions.

Admin

* All Viewer capabilities.
* Add Team.
* Edit Team.
* Delete Team.
* Generate Report and other existing Admin-only operations.

Frontend restrictions must also be enforced by the backend/API.

Do not hard-code the Entra Group Object ID in source code.

⸻

3. Team Administration

Keep the name:

Team Administration

Place it under Release Activities.

Do not create a separate Administration section.

Remove the summary/statistics cards such as:

* Total Teams
* Teams with Kubernetes
* Teams with CX
* Project Keys count
* similar statistics

Keep the page focused on managing teams.

Team actions

* Viewer → View only.
* Admin → Add, Edit and Delete.
* Add Team must be Admin-only.
* Edit/Delete must be Admin-only.
* Backend authorization must enforce this as well.

⸻

4. Team Configuration

Replace team_config.yaml completely with DB-backed configuration.

First migrate all valid existing team configuration into the DB, then remove the YAML and all runtime references to it.

All team configuration must be editable through Team Administration.

Important configuration includes:

* Team Name
* Display Name
* App ID
* Application Fixed Team
* Project Keys
* Repository / Repo Overrides
* Default Branch
* Default Branch Overrides
* Release Branch Suffix Overrides
* Library Section
* Excluded Components Default
* Kubernetes Host
* Kubernetes Namespaces
* other currently required team-specific configuration

Support multiple values wherever required, especially:

* Project Keys
* Default Branch Overrides
* Release Branch Suffix Overrides
* Kubernetes Namespaces
* Excluded Components Default
* Repo Overrides, if applicable.

For Excluded Components Default, do not use a simple comma-separated text field because existing values can contain commas/spaces/hyphens. Use a better UI such as add/remove tags or individual values.

⸻

5. Remove Unnecessary Team Fields

Completely remove these fields/configurations if they are no longer used:

* Primary Project
* Report Template
* OG Identifier
* CG Identifier
* Scrum Teams
* CeedX Filters
* Excluded Components No Code
* all iTrack-related configuration

Do not merely hide unused fields. Remove unnecessary code/database/configuration references as well.

CeedX

Where CeedX-related functionality exists, Application Fixed Team is mandatory because it identifies which team the application belongs to.

⸻

6. Additional Configurations

Use:

Additional Configurations

Do not call it “Advanced Configuration” or “Advanced Configuration - Kubernetes”.

Move optional configuration into this section, including:

* Default Branch Overrides
* Release Branch Suffix Overrides
* Kubernetes configuration
* other genuinely optional configuration.

Kubernetes is optional for a team.

Do not make Kubernetes configuration mandatory for every team.

⸻

7. Kubernetes

Keep existing Kubernetes functionality.

Support:

* Kubernetes Host
* multiple namespaces
* existing required Kubernetes configuration.

Do not store Kubernetes credentials/tokens/secrets in the DB or UI.

Use the existing approved Key Vault/secret-reference mechanism.

For teams without Kubernetes configuration, the application should continue working for non-Kubernetes functionality.

Deployment section

For a Kubernetes-enabled team, continue showing the existing environment/namespace/deployment information.

For a team without Kubernetes configuration, instead of showing an unnecessary Refresh Deployment Status action, show a clear message such as:

Configure Kubernetes details in Team Administration to view deployment status.

⸻

8. Generate Report Connections

When Generate Report is opened, show the common connections:

* GitHub
* Jira Cloud
* Snowflake

Do not show a broad/global Kubernetes connection status at the top.

Kubernetes is team-specific.

When a team is selected, use Kubernetes only if that team has Kubernetes configuration.

Snowflake

Snowflake is currently frequently showing as not connected and Generate Report is taking too long.

Analyze the actual root cause and fix it.

Check for things such as:

* connection handling;
* connection recreation;
* timeout;
* retry behavior;
* configuration;
* connection pooling;
* unnecessary waits.

Do not simply increase timeouts without understanding the problem.

Ensure Snowflake works reliably and Generate Report does not have unnecessary delays.

⸻

9. Previous Branch / Kubernetes

When:

* Team is selected;
* Fix Version is selected;
* Load Components is executed;

the Previous Branch should use Kubernetes PROD information as the primary source, as per the existing intended behavior.

Currently it appears to fall back to date-range logic.

Analyze why Kubernetes lookup is not working and fix it.

Expected:

Kubernetes PROD information
        ↓
Previous Branch
        ↓
Date-range fallback only when Kubernetes information is genuinely unavailable

Do not remove the fallback mechanism.

⸻

10. Review Commits

Remove the Validation Only switch completely.

Only keep:

Violations Only

When Violations Only is enabled:

* show the commit selection checkboxes;
* show Teams Notifications/action controls as applicable.

Do not create another validation switch or duplicate control.

⸻

11. Configuration Standardization

Review the existing .env and compare it with:

* AADLC application configuration;
* Azure App Service App Settings JSON;
* configuration conventions used by other AADLC applications.

Move required centralized configuration from .env into the approved Azure App Service App Settings JSON.

Do not blindly copy every .env property.

Only move configuration that is actually required.

Secrets must remain through the approved Key Vault/secret mechanism.

⸻

12. Local Development + Azure Deployment

The application must work cleanly in both environments:

Local

A developer should be able to clone the branch and run the application locally using the same general configuration convention followed by other AADLC applications.

Review how other applications use mechanisms such as:

* config.yaml
* secrets.yaml
* secrets.yaml.example
* equivalent local configuration patterns.

Do not blindly introduce these files if they are not appropriate for our application. Follow the existing AADLC standard.

Only the configuration that cannot come automatically from Azure App Service should need to be supplied locally.

Azure App Service

When deployed to Azure App Service:

* App Service configuration must load correctly;
* Key Vault references must work;
* application should not depend on a developer’s .env;
* required configuration must be picked up automatically.

The local and Azure configuration approaches should be consistent with the conventions used by the other AADLC applications.

⸻

13. Common Connections / App Configuration

GitHub, Jira Cloud and Snowflake are common/shared connections.

Review the existing Azure App Service App Settings JSON first.

* Reuse existing GitHub configuration.
* Reuse existing Snowflake configuration.
* Add missing Jira Cloud configuration if required.
* Avoid duplicating common configuration per team.

⸻

14. Team Configuration Loading

All existing references to team_config.yaml must be replaced with DB-backed configuration.

Avoid unnecessary repeated DB calls.

Load the required team configuration once for an operation and reuse it.

Do not introduce a long-lived stale cache.

When Admin updates team configuration, subsequent operations must use the latest DB values without requiring an application restart.

⸻

15. Database Design

Design the team configuration DB structure so it can support approximately 25 teams and future configuration additions.

It must properly support multiple values and future expansion without requiring major redesign for every new field.

Keep common/global configuration separate from team-specific configuration where appropriate.

Do not store credentials/secrets in team configuration tables.

⸻

16. Essential Cleanup

Remove completely:

* team_config.yaml runtime dependency;
* Developer role;
* iTrack;
* Primary Project;
* Report Template;
* OG/CG identifiers;
* Scrum Teams;
* CeedX Filters;
* Excluded Components No Code;
* obsolete configuration/environment variables;
* unused references/imports.

Do not leave obsolete code commented out.

⸻

17. Final Validation

Before completing, verify:

* [ ]	Existing application functionality still works.
* [ ]	Admin/Viewer authorization works.
* [ ]	Developer role is completely removed.
* [ ]	Admin group is correctly integrated.
* [ ]	Team Administration is under Release Activities.
* [ ]	Viewer is read-only.
* [ ]	Add/Edit/Delete are Admin-only at frontend and backend.
* [ ]	Unnecessary Team Administration statistics are removed.
* [ ]	team_config.yaml is fully migrated to DB and no runtime references remain.
* [ ]	Team configuration is editable from UI.
* [ ]	Multiple-value fields work correctly.
* [ ]	Kubernetes remains optional.
* [ ]	No Kubernetes credentials are stored in DB/UI.
* [ ]	Generate Report connections work.
* [ ]	Snowflake connection/performance issue is properly analyzed and fixed.
* [ ]	Previous Branch correctly uses Kubernetes PROD information first.
* [ ]	Review Commits has only the Violations Only switch.
* [ ]	Non-Kubernetes teams receive the appropriate deployment-status configuration message.
* [ ]	.env configuration is aligned with AADLC standards.
* [ ]	Local application starts successfully.
* [ ]	Azure App Service configuration loads correctly.
* [ ]	Application deploys successfully to Azure App Service.
* [ ]	No unrelated functionality is broken.

If you encounter anything ambiguous, especially around existing AADLC conventions or configuration behavior, ask me before making assumptions or deleting something that may still be required.
